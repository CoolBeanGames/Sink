<Window x:Class="Sink.BugReportWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Report a bug" Width="460" Height="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False" Background="#161A22">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel><TextBlock Text="REPORT A BUG" Foreground="#8B7CFF" FontSize="10" FontWeight="SemiBold"/><TextBlock Text="What's going wrong?" FontSize="20" FontWeight="SemiBold" Margin="0,5,0,0"/></StackPanel>

        <TextBlock Grid.Row="1" Text="TITLE" Foreground="#7D8493" FontSize="10" FontWeight="SemiBold" Margin="0,18,0,6"/>
        <TextBox x:Name="TitleBox" Grid.Row="2" Height="36" Padding="10,7" Foreground="#F4F6FA" Background="#0F1218" BorderBrush="#353C49" CaretBrush="#F4F6FA"/>

        <TextBlock Grid.Row="3" Text="WHAT HAPPENED" Foreground="#7D8493" FontSize="10" FontWeight="SemiBold" Margin="0,16,0,6"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="4" Padding="10,7" Foreground="#F4F6FA" Background="#0F1218" BorderBrush="#353C49" CaretBrush="#F4F6FA"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <Border Grid.Row="5" Background="#12151C" BorderBrush="#262C38" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,14,0,0">
            <StackPanel>
                <TextBlock Text="AUTO-TAGGED FROM WHERE YOU ARE" Foreground="#6E7584" FontSize="9" FontWeight="SemiBold"/>
                <TextBlock x:Name="TagsText" Foreground="#9D91FF" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Background="#242A34" Foreground="#D1D5DD" BorderThickness="0" Padding="14,8" Margin="0,0,8,0"/>
            <Button x:Name="SubmitButton" Content="Submit" Click="Submit_Click" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
